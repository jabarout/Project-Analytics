/**
 * Development environment.
 * Backend API is expected on local Spring Boot (see Configuration documentation).
 */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080/api/v1',
  applicationName: 'Project Analytics',
};
