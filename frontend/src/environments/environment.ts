/**
 * Default environment (production build).
 * Frontend configuration must remain free of secrets.
 */
export const environment = {
  production: true,
  apiBaseUrl: '/api/v1',
  applicationName: 'Project Analytics',
};
