# N5.1 STOP report — Production Docker Compose

**Date:** 2026-08-20  
**Gate:** N5.1 only — **STOP** (do not start N5.2 automatically)

## 1. Files changed

| File | Change |
|------|--------|
| `docker/docker-compose.prod.yml` | **New** production overlay |
| `frontend/docker/nginx.conf` | Proxy `/api/` → `http://backend:8080/api/` |
| `docs/ops/n5.1-prod-compose.md` | Minimal run instructions |
| `docs/ops/n5.1-prod-compose-report.md` | This report |

## 2. Production compose architecture

- **Base** `docker/docker-compose.yml` unchanged in role (Postgres, Redis, backend, frontend).
- **Overlay** `docker/docker-compose.prod.yml`:
  - Forces `SPRING_PROFILES_ACTIVE=prod`
  - Requires `JWT_SECRET`, `CREDENTIALS_ENCRYPTION_KEY`, `CORS_ALLOWED_ORIGINS`, DB credentials (no secret defaults)
  - Forces `OPENAPI_PUBLIC=false`, `OP_ALLOW_ENV_API_KEY_FALLBACK=false`
  - Isolated names: `pa-prod-*`
  - Isolated host ports via `ports: !override`: **5433 / 6380 / 8081 / 8089**
  - Reports volume `prod_reports_data` → `/data/reports`
- Frontend production build keeps relative `/api/v1`; nginx reverse-proxy makes Docker UI work.

## 3. Production environment variables identified

**Validator-required:** `JWT_SECRET`, `CREDENTIALS_ENCRYPTION_KEY`, `CORS_ALLOWED_ORIGINS`  
**Stack-required:** `DB_NAME`, `DB_USERNAME`, `DB_PASSWORD`, `SPRING_PROFILES_ACTIVE=prod`  
**Pass-through:** Redis password, report/snapshot retention, OAuth URL vars, sync flags  
**Not in N5.1:** SMTP / full env-template work (N5.2)

## 4. `docker compose … config` result

**PASS** — config rendered successfully with overlay + smoke env.  
Published ports after `!override`: 5433, 6380, 8081, 8089 only (no clash with local 5432/8080).

## 5. Actual service startup result

**PASS**

```text
pa-prod-postgres  healthy
pa-prod-redis     healthy
pa-prod-backend   healthy
pa-prod-frontend  started
```

## 6. Health-check results

| Check | Result |
|-------|--------|
| `GET http://localhost:8081/actuator/health` | `{"status":"UP",...}` |
| `GET http://localhost:8089/health` | `OK` |
| Login via frontend proxy `POST /api/v1/auth/login` | **200** + token |

## 7. Production profile verification

Backend log:

- `The following 1 profile is active: "prod"`
- `Production security startup checks passed.`

**Negative check:** recreating backend with the **dev default** `JWT_SECRET` →  
`IllegalStateException: JWT_SECRET must not use the development default secret in the prod profile.`  
(`Application run failed` — validator not bypassed.)

## 8. Remaining N5.1 issues

None blocking. Notes:

- OpenAPI/Swagger URLs return **401** under security filter (not a public docs surface); prod profile still disables springdoc.
- Prod smoke stack was **torn down** after verification (`docker compose -p project-analytics-prod … down`).
- Smoke secrets lived only in `/tmp/pa-n51-prod.env` (not committed).

---

## STOP

**N5.1 complete.** Do not start N5.2 until explicitly requested.
