# N5.2 STOP report — Environment template

**Date:** 2026-08-20  
**Gate:** N5.2 only — **STOP** (do not start N5.3 automatically)

## 1. Files changed

| File | Change |
|------|--------|
| `.env.example` | Split **LOCAL / DEVELOPMENT** vs **PRODUCTION**; document validator + compose required keys; no real secrets |
| `docs/ops/n5.2-env-template.md` | Short operator note |
| `docs/ops/n5.1-prod-compose.md` | Link to N5.2 template |
| `docs/ops/n5.2-env-template-report.md` | This report |

## 2. Keys cross-checked

Against `ProductionSecurityValidator`, `application.yml` / `application-prod.yml`, and `docker/docker-compose.prod.yml`.

**Compose `${VAR:?…}` required:**  
`JWT_SECRET`, `CREDENTIALS_ENCRYPTION_KEY`, `CORS_ALLOWED_ORIGINS`, `DB_NAME`, `DB_USERNAME`, `DB_PASSWORD` — all present in `.env.example` PRODUCTION section.

**Also documented for prod:** profile/OpenAPI/OP fallback flags, isolated `PROD_*` ports, OAuth/frontend URL placeholders, report path, SMTP placeholders, `REGISTRATION_ENABLED`.

## 3. Verification

Scripted check: every prod-compose required key appears in the PRODUCTION section; local JWT default remains only in the local block and is called out as forbidden for prod. **PASS.**

## 4. Remaining N5.2 issues

None.

---

## STOP

**N5.2 complete.** Do not start N5.3 until explicitly requested.
