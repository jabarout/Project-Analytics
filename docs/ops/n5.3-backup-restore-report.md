# N5.3 STOP report — Backup & restore

**Date:** 2026-08-20  
**Gate:** N5.3 only — **STOP** (do not start N5.4 automatically)

## 1. Files changed

| File | Change |
|------|--------|
| `scripts/backup-postgres.sh` | Default `REPORT_STORAGE_PATH` → `./backend/data/reports` (repo-root) |
| `scripts/restore-postgres.sh` | Same default alignment |
| `.env.example` | Document local vs Docker report paths |
| `docs/ops/runbooks/RB-006-restore-from-backup.md` | Quick backup/restore boxes + Docker note |
| `docs/ops/n5.3-backup-restore.md` | Short N5.3 note |
| `docs/ops/n5.3-backup-restore-report.md` | This report |

## 2. Dry-run results

Against local `pa-postgres` (healthy):

| Check | Result |
|-------|--------|
| `./scripts/backup-postgres.sh` | **PASS** — created `data/backups/<stamp>/` |
| `postgres.dump` | Present, non-empty (~83KB) |
| `pg_restore --list` | **PASS** — valid custom-format dump (~170 TOC lines) |
| `reports.tgz` (with corrected path) | **PASS** — includes report files under `reports/` |
| Full DB overwrite restore | **Not run** against live data (destructive); dump validated via `--list` |
| Restore usage errors | Script correctly rejects missing args / missing directory |

## 3. Consistency

- Backup + restore scripts share the same path defaults and Docker `pa-postgres` fallback.
- RB-006 documents procedure, policy (DB + report files; Redis not restored), and Docker caveats (`pa-prod-postgres` naming).

## 4. Remaining N5.3 issues

- Host scripts still auto-detect container name `pa-postgres` only (not `pa-prod-postgres`). Documented in RB-006; prod backups can use host port `5433` with client tools or a one-line exec target change later if needed.

---

## STOP

**N5.3 complete.** Do not start N5.4 until explicitly requested.
