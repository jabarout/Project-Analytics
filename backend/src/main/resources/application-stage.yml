# Staging profile: production-like, non-secret defaults only.
spring:
  jpa:
    show-sql: false

management:
  endpoint:
    health:
      show-details: when_authorized

logging:
  level:
    root: INFO
    com.projectanalytics: INFO
