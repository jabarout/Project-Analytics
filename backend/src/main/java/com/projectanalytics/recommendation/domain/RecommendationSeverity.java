package com.projectanalytics.recommendation.domain;

/**
 * Recommendation severity used for priority ranking (lower ordinal = higher priority).
 */
public enum RecommendationSeverity {
    CRITICAL,
    HIGH,
    MEDIUM,
    LOW
}
